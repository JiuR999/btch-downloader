[build]
  # 构建命令
  command = "npm run build"
  
  # 发布目录 (index.html 所在目录)
  publish = "."

[build.environment]
  # Node.js 版本
  NODE_VERSION = "20"

# 处理 SPA 路由 (如果需要)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
